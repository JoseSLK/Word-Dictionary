package view;

/**
 * The MainWindow class is the entry point for the dictionary application.
 * It creates an instance of the MainApp class to launch the main user interface.
 */
public class MainWindow {
    public static void main(String[] args) {
        MainApp frame = new MainApp();
        frame.main();
    }
}
